telegram:
  token: ${BOT_TOKEN}
  username: ${BOT_NAME}
  path: ${BOT_PATH}
spring:
  datasource:
    url: ${DATABASE_URL}
    username: ${DATABASE_USERNAME}
    password: ${DATABASE_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
server:
  port: ${PORT}

